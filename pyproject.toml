[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "reduce-repo"
version = "0.1.0"
requires-python = ">=3.11"

[project.scripts]
reduce-repo = "reduce_repo:main"

[tool.pytest.ini_options]
testpaths = ["test"]
